export default class Crediti extends Phaser.Scene {

    constructor() {
        super({
            key: "Crediti"
        })
    }

    init() {

    }

    create() {
        
    }

    update(time: number, delta: number): void {
        
    }
}